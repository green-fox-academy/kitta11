//function to generate rgba string based on dynamically generated r, g, b, a codes


export function colorConcate(r: number, g: number, b: number) {

  let gradColor: string = `rgba(${r}, ${g}, ${b})`;
  return gradColor
}